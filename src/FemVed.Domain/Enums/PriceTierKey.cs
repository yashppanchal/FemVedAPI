namespace FemVed.Domain.Enums;

/// <summary>Pricing tier keys for library videos. Each tier has fixed prices per region.</summary>
public enum PriceTierKey
{
    Movie,
    Standard,
    Medium,
    Large
}
